name: Matrix

on: 
    workflow_dispatch:

jobs:
    job:
        runs-on: ubuntu-latest
        strategy:
            matrix:
              version: [18,20]
        steps:
            - name: install node
              uses: actions/setup-node@v6
              with :
                node-version: ${{matrix.version}}


